apply plugin: 'java'


repositories {
    mavenCentral()
}

dependencies {
    //compile "junit"
    testCompile "junit:junit:4.11" 
}

sourceSets {
   main {
      java {
         srcDir 'src/main'
      }
   }
   test {
       java.srcDir file('src/test')
   }
}
